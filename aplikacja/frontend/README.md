# Kanban Node.js Backend

## Setup

1. Install dependencies:
```
npm install
```

2. Configure database connection in `.env` file.

3. Start the server:
```
npm start
```

## API Endpoints

- POST `/api/register` - Register a new user
- POST `/api/login` - Login existing user
